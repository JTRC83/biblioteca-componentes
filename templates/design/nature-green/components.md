# Nature Green — Componentes

## Botones

- **btn-get-started-gradient**
  Adaptacion: Cambiar gradiente del boton a `linear-gradient(135deg, #2d6a4f, #52b788)`. Texto blanco, radius 12px, font-family DM Sans, weight 600. Hover: gradiente a `linear-gradient(135deg, #1b4332, #2d6a4f)` con lift -1px.

- **btn-copy-link**
  Adaptacion: Color de icono y texto a `#2d6a4f`. Background transparent con border 1px `#52b788`. Hover: background `#f0f5ee`. Radius 12px.

## Cards

- **card-neumorph**
  Adaptacion: Background `#f5f7f3` con sombra neumorfica verde suave: `8px 8px 16px rgba(45,106,79,0.08), -8px -8px 16px rgba(255,255,255,0.8)`. Radius 16px. Titulos en DM Sans weight 700, color `#1a2e1a`.

- **card-features**
  Adaptacion: Surface blanca `#ffffff`, border 1px `#dfe6da`, radius 16px, padding 28px. Iconos con background `#eef2ea` y color `#2d6a4f`. Hover: sombra `0 4px 16px rgba(26,46,26,0.08)` + lift -2px. Texto descriptivo en `#4a6a4a`.

- **card-leaf-tiles** (patron)
  Adaptacion: Usar el patron de tiles de hojas con tonos `#2d6a4f`, `#52b788` y `#95d5b2`. Opacidad reducida (0.06-0.1) sobre fondo `#f5f7f3` para efecto sutil y organico.

## Checkboxes & Radios

- **cb-animated-check**
  Adaptacion: Color del check animado a `#2d6a4f`. Background del checkbox en blanco con border `#dfe6da`. Al activar: background `#2d6a4f`, check blanco. Radius 8px.

- **cb-drawn-tick**
  Adaptacion: Trazo del tick dibujado a mano en color `#52b788`. Stroke width moderado. Al activar: animacion organica que recuerda crecimiento natural.

- **radio-card**
  Adaptacion: Card seleccionable con border `#dfe6da` en estado inactivo. Seleccionado: border `#2d6a4f` con ring `0 0 0 3px rgba(45,106,79,0.15)`. Radius 16px. DM Sans weight 600 para labels.

## Inputs & Forms

- **form-modern**
  Adaptacion: Inputs con border 1px `#dfe6da`, radius 12px. Focus: border `#2d6a4f` + ring `rgba(45,106,79,0.15)`. Labels en `#4a6a4a` weight 500. Boton submit en verde bosque `#2d6a4f`.

- **input-floating**
  Adaptacion: Label flotante en color `#8aa388` (muted). Al focus: label sube y cambia a `#2d6a4f`. Input border-bottom 2px `#dfe6da` que pasa a `#52b788` en focus. Fuente DM Sans.

- **input-underline**
  Adaptacion: Input minimal con underline. Border-bottom 1px `#dfe6da`, color texto `#1a2e1a`. Focus: border-bottom 2px `#2d6a4f`. Placeholder en `#8aa388`.

## Loaders

- **loader-pulse-ring**
  Adaptacion: Anillos pulsantes en `#52b788` con opacidad decreciente. Centro solido en `#2d6a4f`. Animacion organica que evoca ondas en agua o crecimiento natural.

- **loader-liquid-progress**
  Adaptacion: Liquido en gradiente `linear-gradient(180deg, #52b788, #2d6a4f)`. Onda suave organica. Borde del contenedor en `#dfe6da`, radius 9999px. Texto de porcentaje en DM Sans weight 600, color `#1a2e1a`.

## Backgrounds & Patterns

- **bg-aurora**
  Adaptacion: Aurora con tonos `#2d6a4f`, `#52b788` y `#95d5b2`. Movimiento lento y organico. Opacidad baja (0.15-0.25) sobre fondo `#f5f7f3`. Evoca luz natural filtrada entre hojas.

- **pat-leaf-tiles**
  Adaptacion: Patron de tiles de hojas en verde bosque `#2d6a4f` con opacidad 0.04-0.08 sobre fondo `#f5f7f3`. Tiles organicos que se repiten sutilmente. Perfecto para secciones de fondo.

- **pat-dots**
  Adaptacion: Puntos en `#52b788` con opacidad 0.1, espaciado 24px sobre fondo `#f5f7f3`. Grid regular que da textura organica sin distraer.

- **pat-grid-fade**
  Adaptacion: Grid con lineas `#dfe6da` que se desvanecen hacia los bordes. Opacidad maxima 0.15 en el centro. Evoca estructura natural y crecimiento ordenado. Sobre fondo `#f5f7f3`.